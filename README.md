# Caloriq
Caloriq is a calorie estimation app where users can search for dishes and instantly get calorie and nutritional info per serving using the USDA FoodData Central API. It also supports user authentication, motivational elements, and a clean, responsive UI.
